<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.fuer_xitong.mapper.ProfessionalGroupMemberMapper">

    <insert id="insertMember">
        INSERT INTO professional_group_member (
            department_id,
            keshi,
            group_path,
            person_type,
            name,
            academic_position,
            talent_title,
            roles,
            resume_file_path,
            gcp_cert_path,
            practice_cert_path,
            create_by,
            create_time
        ) VALUES (
                     #{departmentId},
                     #{keshi},
                     #{groupPath},
                     #{personType},
                     #{name},
                     #{academicPosition},
                     #{talentTitle},
                     #{roles},
                     #{resumeFilePath},
                     #{gcpCertPath},
                     #{practiceCertPath},
                     #{createBy},
                     NOW()
                 )
    </insert>



    <select id="selectByDepartmentAndGroup"
            resultType="org.example.fuer_xitong.pojo.vo.ProfessionalGroupMemberVO">

        SELECT
            id,
            department_id      AS departmentId,
            keshi,
            group_path         AS groupPath,
            person_type        AS personType,
            name,
            academic_position  AS academicPosition,
            talent_title       AS talentTitle,
            roles,
            resume_file_path   AS resumeFileUrl,
            gcp_cert_path      AS gcpCertUrl,
            practice_cert_path AS practiceCertUrl,
            create_by          AS createBy,
            create_time        AS createTime
        FROM professional_group_member
        WHERE department_id = #{departmentId}
          AND group_path = #{groupPath}
        ORDER BY create_time DESC

    </select>

    <select id="selectById"
            resultType="org.example.fuer_xitong.pojo.vo.ProfessionalGroupMemberVO">
        SELECT
            id,
            department_id      AS departmentId,
            keshi,
            group_path         AS groupPath,
            person_type        AS personType,
            name,
            academic_position  AS academicPosition,
            talent_title       AS talentTitle,
            roles,
            resume_file_path   AS resumeFileUrl,
            gcp_cert_path      AS gcpCertUrl,
            practice_cert_path AS practiceCertUrl,
            create_by          AS createBy,
            create_time        AS createTime
        FROM professional_group_member
        WHERE id = #{id}
    </select>

    <delete id="deleteById">
        DELETE FROM professional_group_member
        WHERE id = #{id}
    </delete>


</mapper>
